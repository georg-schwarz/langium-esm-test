{
  "name": "test",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "test/src",
  "projectType": "library",
  "tags": [],
  "// targets": "to see all targets run: nx show project test --web",
  "targets": {
    "build": {
      "dependsOn": ["swc"],
      "command": "npx tsc-esm-fix --target dist/test"
    },
    "swc": {
      "executor": "@nx/js:swc"
    }
  }
}
